---
description: >
  Job to setup cookbooks enviroment. 
  Note: If a ssh key is needed, please add an environment 
  variable called KITCHEN_SSH_KEY in a circleci context 
  containing the base64 string of the ssh key private key. 
executor: chefdk
steps:
  - checkout
  - git_checkout:
      url: "git@github.com:ClouDesire/ci-conf.git"
  - cookbooks_setup
  - cookbooks_foodcritic_test
