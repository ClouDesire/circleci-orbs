---
description: >
  Job to execute chef kitchen suites
parameters:
  suites:
    type: string
    default: ""
  concurrency:
    type: boolean
    default: true
executor:
  name: chefdk
  resource_class: small
steps:
  - checkout
  - git_download_repo:
      repo: ci-conf
      branch: master
      dir: "."
  #- kitchen_test:
  #    suites: << parameters.suites >>
