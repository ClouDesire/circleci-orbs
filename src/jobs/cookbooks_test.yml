---
description: >
  Job to execute chef kitchen suites
parameters:
  suites:
    type: string
    default: ""
  concurrency:
    type: boolean
    default: true
executor:
  name: chefdk
  resource_class: small
steps:
  - checkout
  - git_checkout:
      url: "git@github.com:ClouDesire/ci-conf.git"
      branch: m
      git_email: circleci@cloudesire.com
      git_username: CircleCI
  #- kitchen_test:
  #    suites: << parameters.suites >>
