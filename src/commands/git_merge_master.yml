---
description: >
  Make sure that built branch is aligned to default branch (master/main)
steps:
  - run:
      name: Setup git
      command: <<include(scripts/git_setup.sh)>>
  - run:
      name: "Merge with master"
      command: <<include(scripts/git_merge_master.sh)>>
