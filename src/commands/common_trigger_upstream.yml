description: "Execute Foodcritic Test"
parameters:
  projects:
    type: string
    default: "" 
  cookbook_branch:
    type: string
    default: master
steps:
  - run:
      environment:
        projects: <<parameters.projects>>
        cookbook_branch: <<parameters.cookbook_branch>>
      name: Trigger upstream builds
      command: <<include(scripts/trigger_build.sh)>>
        

        