---
description: "Setup steps"
parameters:
  sonar_version:
    type: string
    default: "4.2.0.1873"
  sonar_dir:
    type: string
    default: "/tmp/cache/sonar-scanner"
  sonar_opts:
    type: string
    default: ""
  cache_version:
    type: integer
    default: 1
steps:
  - run:
      name: Get SonarQube Server Version
      command: <<include(scripts/sonar_get_server_version.sh)>>
  - restore_cache:
      keys:
        - sonarcli-<< parameters.sonar_version >>_sonarqube-{{ .Environment.SONAR_SERVER_VERSION }}_v<<parameters.cache_version>>
  - run:
      environment:
        SONAR_VERSION: <<parameters.sonar_version>>
        SONAR_DIR: << parameters.sonar_dir >>
        SONAR_OPTS: << parameters.sonar_opts >>
      name: Execute sonar
      command: <<include(scripts/sonar.sh)>>
  - save_cache:
      key: sonarcli-<< parameters.sonar_version >>_sonarqube-{{ .Environment.SONAR_SERVER_VERSION }}_v<<parameters.cache_version>>
      paths: ~/.sonar
